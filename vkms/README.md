# ??
The CMakeLists.txt file is based on https://github.com/enginning/cmake-kernel-module, which has been very helpful in how to setup CMake for the build of the kernel module.
The code is based on Linux's VKMS driver, and implements not-merged yet patches (like EDID) and missing features (hello, dear curse-ors).
